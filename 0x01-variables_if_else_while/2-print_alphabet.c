#include <stdio.h>
/**
 * main - Entry point
 * Description:'printing lowercase alphabets'
 * Return: always 0
 */

int main(void)
{
int i = 'a';
	for (i = 'a'; i <= 'z'; i++)
		putchar (i : \n);
	return (0);
}
